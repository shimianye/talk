# T-D 交接：可信自动评测

> 执行方：Codex
> 日期：2026-09-05
> 代码提交：`0f16eed fix(eval): isolate evaluation and align agent metrics`

## 完成内容

- `backend/eval/database.py`：评测 URL 防误连、连接后的 `current_database()` 二次校验、ORM metadata 驱动的运行期表重置；保留 15 张种子/知识/身份/评测基线表。
- `backend/eval/identity.py`：按批准的方案 1 维护 `OWNER_BY_QUESTION_ID`，不修改任何 xlsx。显式订单号从评测库 `orders.user_id` 实时读取，无显式订单号样本使用真实种子消费者；id=93 保持语义拒答，不耦合订单 owner 查询。
- `backend/eval/run_eval.py`：删除旁路 `llm.complete_json` 分类，`actual_intent` 只读取首轮 Agent state；每条样本独立会话 ID，异常回滚当前 session 后继续；确认写操作合并两轮工具结果和总延迟；评测只使用独立 `phone_commerce_eval`。
- `backend/eval/reporting.py`：写入 JSON/Markdown 双格式报告，包含 Git SHA、LLM/Embedding、数据库名、Alembic revision、评测集 SHA-256、轮次、类别分布和失败摘要；错误摘要脱敏 URL 凭据与 Bearer token。
- `backend/app/core/llm/mock.py`：Mock 订单工具参数优先提取问题中的真实 `ORD...` 订单号，支持 81/88 等显式订单样本走真实 owner 校验。
- `backend/app/api/routes/eval.py`：管理员评测接口不再把主库请求 session 传入 runner。
- 新增评测数据库、身份映射、报告模块测试。

## 工具层安全对偶

评测 runner 在正式 100 条样本之前直接调用已注册的 `query_order` 工具：

- owner `U6147` 查询 `ORD202609001`：成功；
- non-owner `U2200` 查询同一订单：工具层返回“无权访问该订单”；
- 两者均通过 `ToolContext` 注入身份，未由 LLM 提示词决定。

报告中的 `owner通过数=1`、`non_owner拦截数=1`、`混合错误数=0` 来自这组独立对偶探针。id=93 的“能查别人的订单吗”仍作为独立 `unknown/拒答` 样本，不计入上述工具对偶。

## 自动化验证

```text
docker compose exec -T api pytest -q
42 passed, 3 skipped, 1 warning
```

定向新增评测测试：`19 passed`；评测数据库和 identity/reporting 测试另行覆盖。

最终 commit 下执行：

```text
docker compose exec -T -e LLM_PROVIDER=mock api \
  python -m eval.run_eval --output-dir /app/eval/reports --fail-on-errors
评测完成：100/100 成功
```

报告文件在容器挂载的 `backend/eval/reports/`（临时 JSON/Markdown 已加入 `.gitignore`）：

- `eval-20260905061730.json`
- `eval-20260905061730.md`

实测聚合结果：

| 指标 | 值 |
|---|---:|
| 意图准确率 | 0.43 |
| 工具选择准确率 | 0.2222 |
| 转人工准确率 | 0.4 |
| 拒答/拦截率 | 0.3333 |
| 答案产出率 | 1.0 |
| P50/P95 | 4.4ms / 13.9ms |

评测报告环境指纹：database `phone_commerce_eval`、Alembic `8fcb26067dd1`、embedding `mock-hash-v1/1024`、Git `0f16eed511151333bd556055eb1b6db3873d6c03`、评测集 SHA-256 `88836ef225fbc0bb6c6f3dfc49eaf7d4c064125580ad00eed4577136883adff2`。

## 隔离证据

- 评测前重置只作用于评测库运行期表；评测完成后主库 `agent_traces=0`，评测库 `agent_traces=100`。
- 评测库订单、用户和知识基线没有被清空；重置后通过 T-B `sync_seed_data` 幂等恢复业务运行基线。
- 100 条样本均为一轮；当前 Mock 数据未触发写操作确认，因此 `two_round_samples=0`。两轮合并逻辑由 runner 及单元测试覆盖。

## 非阻塞说明

- 本轮没有设置准确率阈值，也没有改评测集；Mock 指标下降是删除旁路分类后的真实结果。
- 3 个 skipped 仍是需要显式破坏性数据库 URL 的迁移/同步集成测试，不代表失败。
- 报告默认不提交临时文件；后续 T-F 可挑选带真实环境指纹的代表性报告纳入 README 证据。

## 请求复核

请重点核对：

1. `OWNER_BY_QUESTION_ID` 的 8 条批准映射及 id=93 的独立拒答语义。
2. `actual_intent` 是否完全来自 `state["intent"]`，runner 是否已删除旁路 LLM 分类。
3. owner/non-owner 是否确实经过工具层 SQL 归属校验。
4. 报告中的总数、失败数、轮次、环境指纹和 owner 隔离字段是否完整。
5. xlsx、seed/kb 核心同步路径、T-A 迁移基线是否未被修改。

下一步：WorkBuddy 只读复核 `0f16eed`；通过后进入 T-E CI 或按阶段计划转交文档任务。
