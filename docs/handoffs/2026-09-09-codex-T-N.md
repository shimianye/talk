# T-N 求职证据增强交接

## 完成内容

- 新增消费者可见的 `decision_summary`，只包含白名单意图、工具、来源、安全状态、Trace ID 和运行模式。
- 知识库工具完成后把文档来源汇总进 Agent state；工具返回仍先递归脱敏。
- Agent Trace 的工具执行快照改为从真实 `tool_results` 构造，修复执行完成后 `tool_calls` 已清空导致管理端工具数恒为 0 的问题。
- 聊天页增加默认收起的“本次 AI 决策依据”，不展示 Prompt、参数、业务结果或 Chain-of-Thought。
- 管理端评测页增加指标卡、样本/异常摘要、缺失值口径和低指标提醒。
- 固定 24 条真实模型求职评测切片；样本 ID 在运行前固化，报告记录切片内容哈希。
- README 更新真实证据口径，并新增 `docs/INTERVIEW-GUIDE.md`。

## 关键文件

- `backend/app/services/decision_summary.py`
- `backend/app/core/agent/nodes.py`
- `backend/app/services/chat_service.py`
- `backend/eval/job_slice.py`
- `backend/eval/run_eval.py`
- `frontend/src/pages/Chat.tsx`
- `frontend/src/pages/Admin.tsx`
- `docs/INTERVIEW-GUIDE.md`

## 验证

```text
cd backend && python -m pytest -q
47 passed, 3 skipped

cd frontend && npm run build
build passed；最大 JS 178.52 kB，gzip 58.47 kB
```

新增测试覆盖：

- 决策摘要白名单与敏感信息不外泄。
- 空状态不推断工具或来源。
- 知识来源在脱敏后汇总。
- 24 条切片 ID 唯一、跨六类范围、顺序与哈希稳定。

## 真实模型验证

Docker 恢复后已于 2026-09-09 完成一次固定 24 条 DeepSeek 评测，24/24 样本成功执行，没有链路级异常。报告环境指纹：

- git commit：`9fcd521bc2caf71f051fe6ff86b4855179b07a87`
- LLM：`deepseek / deepseek-chat`
- Embedding：`mock / mock-hash-v1`
- 评测库：`phone_commerce_eval`
- 切片哈希：`55ceb6f187f50d3994ffd663de32c34e4b47738575980f7f75c9ff9e82c750a5`

| 指标 | 结果 |
|------|------|
| 意图准确率 | 0.7917（19/24） |
| 工具选择准确率 | 0.5000（2/4） |
| 转人工准确率 | 0.8000（4/5） |
| 拒答/拦截率 | 0.6667（4/6） |
| 答案产出率 | 1.0000（24/24） |
| P50 / P95 | 4447.1 ms / 11083.3 ms |

代表性报告：`backend/eval/reports/eval-20260909133915.md` 与 `.json`。本轮只运行一次，没有根据结果换题；答案产出率不代表事实正确率，Embedding 仍为 Mock。

复现命令：

```powershell
cd D:\develop\phone-commerce-agent
docker compose up -d --build
docker compose exec api python scripts/bootstrap.py
docker compose exec api python -m eval.run_eval --job-slice
```

运行前应确认 `api` 容器读取 `LLM_PROVIDER=deepseek`。重新运行会产生新的外部模型调用，应保留新的时间戳报告，不覆盖本轮基线。

## 复核重点

1. 消费者响应中不得出现工具参数、订单数据、用户 ID 或内部 Prompt。
2. `decision_summary.actions` 必须来自真实工具执行结果，不来自模型计划。
3. Mock 与真实模型指标继续分开展示。
4. 不把当前字符 SSE 改称后端真流式。
