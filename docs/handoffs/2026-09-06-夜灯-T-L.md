# 交接 · 2026-09-06 · 夜灯 → T-L 技术决策文档 v2（SSE 章节）

> 写给失眠夜 / Codex 的共享读端。基于 T-H 最终态 `4d0748c` 回溯，未改业务代码。

## 本次交付

新增 `docs/decisions/05-sse-streaming.md`（第 5 章），5 条决策，每条按「为什么选 / 放弃什么 / 何时失效 / 实现位置」四段式：

| 决策 | 主题 | 关键实现 |
|------|------|----------|
| D5-1 | fetch + ReadableStream 解析 SSE（而非 EventSource） | `api.ts::chatStream`（POST + Authorization 约束） |
| D5-2 | 前端打字机式渲染，不宣称真流式 | `Chat.tsx` 消费流；后端 `chat.py::chat_stream` 未改 |
| D5-3 | token 显示队列 30ms/字符 + loading 同步 | `Chat.tsx::drainTokenQueue` / `streamDoneRef` / `finishLoadingRef` |
| D5-4 | 同步 `api.chat()` 保留 + 流式失败降级 | `Chat.tsx::catch`（`!started` 时 fallback） |
| D5-5 | 跨 chunk SSE 解析健壮性 | `api.ts` buffer + `pop()` 残片 |

- 明确口径：**前端打字机式渲染，不宣称真流式、不宣称降低首字延迟**（D5-2 失效条件写清：若要降低首字等待，必须升级后端真流式）。
- 同步更新 `docs/decisions/README.md`：阅读顺序加第 5 章 + 记录基准补充 `4d0748c`。

## 改动范围

仅 `docs/decisions/05-sse-streaming.md`、`docs/decisions/README.md`、`docs/TASK-LEDGER.md`。**未改** app/、frontend/、评测集、Alembic、seed/kb。

## 待复核

1. 5 条决策与 `api.ts` / `Chat.tsx`（`4d0748c`）实现是否一致；
2. 数字与口径是否准确（尤其「伪流式」「不宣称真流式」表述）。
