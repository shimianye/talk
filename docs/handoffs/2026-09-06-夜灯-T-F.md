# T-F 交接：README 升级（Mermaid / 指标双栏 / CI badge / 安全与评测节）

> 执行方：WorkBuddy（夜灯）
> 日期：2026-09-06
> 前置：T-E 真实 GitHub 验证通过（Run `34005144560` success，head `429ce8e`）
> 依据：`D:\Administrator\WorkBuddy\outputs\T-F实施计划-README升级.md` + T-D 复核 §四 + T-E 复核 §四

## 复核前置（T-E 真实验证）

Codex 交接的 T-E 真实验证结果，静态层复核通过：

| 项 | 结论 |
|---|---|
| `429ce8e` 改动范围 | 仅 `.github/workflows/ci.yml`（+4 行 apt-get install） |
| `bd27b6a` 改动范围 | 仅 handoff 文档（+14/-1） |
| 业务代码 / 评测集 / Alembic / seed-kb 路径 | 均未触碰 ✅ |
| Run `34005144560` = success | 属行为层，静态无法自证，接受 Codex 报告 |

**首次失败根因归我**：T-E 设计里的 `Wait for Postgres and Redis` step 用了 `pg_isready` / `redis-cli`，
而 `ubuntu-latest` runner 不保证预装这两个客户端 → readiness step 直接失败。
更根本的问题是：service container 的 `--health-cmd` 本已足够，我额外加的 wait step 是冗余保险，
反而引入新的系统依赖，把保险变成了故障点。教训已记入 memory。

## 本次改动

| 文件 | 改动 | 说明 |
|------|------|------|
| `README.md` | 重写（154 行 → 约 300 行） | 见下节 |
| `docs/TASK-LEDGER.md` | T-E 记真实 CI / T-F 记交付内容 / 删除重复 T-D 行 | 失眠夜点名的文档清理项 |
| `docs/screenshots/README.md` | 新增 | 4 张截图的清单与规范，占位用 |
| `docs/superpowers/specs/2026-09-05-python-documentation-design.md` | 丢弃改动 | 纯空格误改（LF/CRLF 残骸），非 T-F 所需 |

## README 重写内容

结构从 11 节旧稿升级为：Header + CI 状态行 → Badges → 项目定位 → **系统架构（Mermaid）** → 技术栈 → 目录结构 → 快速开始 → **测试与评测（指标双栏）** → **安全与权限** → 实现路线图 → **评测方法** → 截图 → 技术决策文档 → License。

**新增：**
- **CI badge** 正式启用（CI 已真绿，不再是占位），URL 用 `shimianye/talk`
- **Mermaid 架构图**：覆盖 用户/前端/API/Agent 9 节点/14 工具/主库/评测库/Redis/CI，三色区分数据层（黄）、评测（蓝）、CI（绿）
- **指标双栏**：6 项指标 + Owner 隔离探针，每项给分母口径
- **安全与权限** 节：含 `_assert_owner` 代码块，说明校验在 SQL 层不依赖 LLM
- **评测方法** 节：评测集构成、身份处理、环境指纹 7 字段、产物、容错

**修正的过期数字：**

| 项 | 旧 | 新 |
|---|---|---|
| 测试数 | 26 | 45 |
| 外键约束 | 13 | 15 |
| 路由数 | 20 | 19 |
| 数据层入口 | `init_db.py`（待 Docker 起库） | `bootstrap.py` 幂等入口，`init_db.py` 兼容保留 |
| LLM badge | `LLM-DeepSeek`（误导，默认其实是 Mock） | 改为 PostgreSQL+pgvector / Alembic 两个更实的 badge |

**同时修正了我自己 T-F 计划里的两个错**：
1. 计划 §七 第 10 项 badge URL 写的是 `<user>/phone-commerce-agent`，实际仓库是 `shimianye/talk`
2. 计划 §四 Mermaid 里误把外键数 15 写成表数，已改为 23 张业务表

## 验证方式

- 数字逐项与代码核对：14 工具 / 5 组、9 agent 节点、19 路由、15 外键、23 业务表、20 篇 kb-docs、45 测试、100 条评测集
- `git check-ignore docs/screenshots/README.md` → 未被忽略，素材会入仓 ✅
- Mermaid 语法人工核对（subgraph / 链式箭头 / classDef 均合规）

**未验证项**：Mermaid 在 GitHub 上的实际渲染效果——沙箱无法渲染，需推送后在 GitHub 页面确认。

## 遗留问题

1. **4 张截图未到位** — T-F 完成定义的最后一块，需失眠夜本机截图后放入 `docs/screenshots/`，规范见该目录 README
2. **`bd27b6a` + 本次改动尚未 push** — 沙箱无 GitHub credential，push 需失眠夜在自己终端执行
3. **本地测试出现 `45 passed, 1 warning`** — 此前记录为 `45 passed, 0 skipped`；warning 内容未查，不阻断但建议下次清理时看一眼
4. **真实 LLM 指标列仍为「待测」** — 需 `DEEPSEEK_API_KEY` 注入后补测，不阻塞 T-F

## 下一步建议

1. 失眠夜 push：`git -C D:\develop\phone-commerce-agent push origin main`
2. push 后确认 GitHub 页面上 Mermaid 渲染正常、CI badge 亮绿
3. 补 4 张截图 → T-F 收尾
4. 之后进 T-G（技术决策文档四章），README「技术决策文档」节的 `docs/decisions/` 链接待 T-G 回填

**不在 T-F 范围**：演示视频 / 压测 / 真实 LLM 指标 / 云部署（T-I/T-J/T-M）。
