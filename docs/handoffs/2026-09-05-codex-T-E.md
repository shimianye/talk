# T-E 交接：GitHub Actions CI

> 执行方：Codex
> 日期：2026-09-05
> 代码文件：`.github/workflows/ci.yml`

## 完成内容

- 新增 GitHub Actions workflow，触发条件为 `push main`、`pull_request main` 和手工 `workflow_dispatch`。
- 使用 `pgvector/pgvector:pg16` 与 `redis:7-alpine` service containers，并同时配置容器 health check 和 runner 主动探测。
- 设置 `LLM_PROVIDER=mock`、`EMBEDDING_PROVIDER=mock`，CI 不读取或注入 DeepSeek 密钥。
- 在 bootstrap 前创建 T-A/T-C 破坏性集成测试所需的两个临时数据库：`phone_commerce_migration_test`、`phone_commerce_bootstrap_test`。这是对建议骨架中遗漏项的必要修正。
- 调用 T-C `scripts/bootstrap.py` 初始化主库与评测库。
- 调用完整 pytest，并调用 T-D `python -m eval.run_eval --output-dir reports --fail-on-errors` 跑 100 条 Mock 评测。
- 使用 `actions/upload-artifact@v4` 上传 `backend/reports/`，保留 30 天；失败时将最近报告摘要写入 GitHub Step Summary。
- 首次 GitHub 运行发现 runner 未提供 `pg_isready`/`redis-cli`，新增 `postgresql-client` 与 `redis-tools` 安装步骤后修复 readiness 阻塞。
- CI workflow 未修改任何业务代码、评测集、迁移或 seed/kb 同步路径。

## 环境变量

workflow 显式注入：

- `DATABASE_URL` / `EVAL_DATABASE_URL`
- `ALEMBIC_DATABASE_URL`（固定 `_migration_test` 库）
- `BOOTSTRAP_TEST_DATABASE_URL`（固定 `phone_commerce_bootstrap_test` 库）
- `REDIS_URL`
- `LLM_PROVIDER=mock` / `EMBEDDING_PROVIDER=mock`
- `KB_DOCS_DIR=${{ github.workspace }}/data/kb-docs`
- `PYTHONPATH=${{ github.workspace }}/backend`

不设置 `DEEPSEEK_API_KEY`，不使用生产 `.env`。

## 本地验证

### YAML 与项目静态检查

```text
python -c "import yaml; yaml.safe_load(open('.github/workflows/ci.yml', encoding='utf-8'))"
docker compose config -q
git diff --check
```

均通过。仓库当前未安装 `actionlint`，因此未做 actionlint 专项检查。

### CI 等价集成测试

在当前 PostgreSQL 容器中创建与 workflow 相同的两个临时库，并注入相同 URL 后执行，结果为：

```text
45 passed, 1 warning in 11.52s
```

其中 45 条包含 T-A 迁移三连、T-B 同步集成、T-C 双库 bootstrap 集成和 T-D 新增单测；没有 skipped。

T-D 100 条 Mock 评测已在 `0f16eed` 后独立复跑通过：`100/100`，报告上传目录兼容 workflow 的 `backend/reports/`。

### GitHub Actions 真实运行

修复提交 `429ce8e fix(ci): install readiness clients before service checks` 已成功 push 到 `main`。

- Run：[#34005144560](https://github.com/shimianye/talk/actions/runs/34005144560)
- Head SHA：`429ce8e91f154dd002c6afac35bd879bcdfcd2a6`
- 结论：`success`
- 关键步骤全部成功：service 初始化、依赖安装、readiness、测试库创建、bootstrap、pytest、100 条 Mock 评测和 artifact 上传。
- Artifact：`eval-report-2-34005144560`，保留 30 天。

首次运行 `1c7a5bf` 失败在 readiness step，原因是 runner 缺少命令行客户端；没有进入 bootstrap、pytest 或评测。该问题已由 `429ce8e` 修复，未修改应用逻辑。

## 设计边界

- 不引入 Docker-in-Docker。
- 不连接 DeepSeek，不消耗 API 预算。
- 不增加 lint/mypy/pyright，保持阶段一已批准的验收范围。
- `pgvector/pgvector:pg16` 假设 GitHub-hosted runner 为 amd64；arm64 迁移另行设计。
- workflow 只上传评测报告 artifact，不修改 README；badge 属于 T-F。

## 请求 WorkBuddy 复核

真实 Run 已成功，T-E 的 CI 闭环完成。请 WorkBuddy 复核 Run `34005144560` 后进入 T-F README、CI badge 与截图工作。
