# T-A 交接：真实 Alembic 初始迁移

> 执行方：Codex  
> 提交：`0e050d6 feat(db): establish real Alembic baseline migration`  
> 状态：待 WorkBuddy 复核

## 完成内容

- 将空的 `8fcb26067dd1` 改为真实初始迁移，覆盖当前 23 张业务表和新增 `sync_manifests`，共 24 张。
- Alembic 成为结构唯一来源；`init_db.py` 改为先执行 `alembic upgrade head`，不再调用 `create_all`。
- ORM metadata 声明 HNSW cosine 索引、知识片段位置唯一约束和三项已验证自然唯一约束。
- Alembic 支持 `ALEMBIC_DATABASE_URL` 覆盖并比较 column type/server default。
- 新增迁移三连集成测试、漂移检查脚本和种子自然键持续验证脚本。

## 关键取舍与修正

- 当前镜像安装的 `pgvector==0.5.0` 实测不存在 `pgvector.alembic` 模块；直接导入会导致 Alembic 启动失败。
- 迁移改为显式导入 `pgvector.sqlalchemy`，并用真实 upgrade 与 drift check 验证 `Vector(1024)`。
- downgrade 删除全部应用表与 HNSW/B-tree 索引，但保留数据库的 `vector` 扩展。
- `prices.variant_id` 表达每个 SKU 一条当前官方指导价；历史价未来拆独立表。

## 验证结果

```text
迁移首次 upgrade: 24 tables, vector extension present, HNSW vector_cosine_ops present
upgrade -> downgrade -> upgrade: passed
downgrade 后: 0 application tables, vector extension retained
alembic check: No new upgrade operations detected
迁移集成测试: 1 passed
现有测试: 26 passed, 1 integration test skipped（未显式提供测试库时）
种子唯一性: prices.variant_id / store_products.sku / eval id/question 全部通过
兼容初始化: 9 products, 31 variants, 100 eval items, 20 docs, 87 chunks
Python compileall: passed
```

测试数据库为 `phone_commerce_migration_test`，与主库隔离。带数据 downgrade 后已恢复到 Alembic head，当前为空结构，供后续复核重复使用。

## 请 WorkBuddy 重点复核

1. ORM 索引/唯一约束与迁移是否一致。
2. upgrade/downgrade 依赖顺序和保留 vector 扩展是否符合规格。
3. `check_migration_drift.py` 与破坏性测试库后缀防护是否足够。
4. T-B 使用 `sync_manifests` 时是否无需再修改初始迁移。

## 遗留与下一步

- 当前 `init_db.py` 的内容导入仍非幂等，重复执行会违反唯一约束；这是 T-B 的明确范围，不应在 T-A 内隐藏处理。
- 当前主 volume 是旧 `create_all` 结构且没有 Alembic version；项目约定不做旧库兼容，T-C 干净环境验收时执行 `docker compose down -v` 重建。
- WorkBuddy 复核通过后，Codex 认领 T-B，实施种子 upsert、manifest 快速路径与知识库四维版本键。
