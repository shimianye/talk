# 交接 · 2026-09-06 · 夜灯 → T-G 技术决策文档 + CI 复验

> 写给失眠夜 / Codex 的共享读端。读完后按 `docs/TASK-LEDGER.md` 状态续作。

## 本次做了什么

### 1. T-G 技术决策文档 v1（主体已交付，待复核）

新增 `docs/decisions/` 目录，共 5 个文件：

| 文件 | 内容 |
|------|------|
| `docs/decisions/README.md` | 索引 + 决策记录统一格式（状态/背景/决策/放弃/失效）+ 状态标记含义 + 数字口径 |
| `docs/decisions/01-architecture.md` | 架构：7 条决策（LangGraph 状态图、LLM 抽象与 Mock 优先、工具注册表与 RBAC、Alembic 迁移、分层与 db 注入、执行上限、环境变量注入） |
| `docs/decisions/02-rag.md` | RAG：6 条决策（混合检索+RRF、零依赖 BM25、Mock/远程 Embedding、中文 bigram 分词、段落切分、检索降级） |
| `docs/decisions/03-security.md` | 安全：7 条决策（双层护栏、PII 脱敏、Owner 隔离、写操作确认、RBAC/JWT/bcrypt、审计降级、执行上限） |
| `docs/decisions/04-evaluation.md` | 评测方法论：7 条决策（独立评测库、身份映射派生、Owner 对偶探针、环境指纹、双格式报告、全分母指标、Mock 基线） |

共 **27 条决策**，每条按「为什么选 + 放弃什么 + 何时失效」三段式写，附真实实现位置（文件名/函数名）。

**写作口径**：全部基于 `3f28f4c`（HEAD）代码回溯，非设计臆想。数字对齐台账：45 测试 / 14 工具（5 组）/ 9 节点 / 19 路由 / 24 张表 + 1 alembic_version / 15 外键 / 100 条评测集。**注：复核时发现「23 业务表」为 T-B 加 sync_manifests 前的旧数字，实际 24 张表，已在本轮一并修正 README/台账/记忆。**

### 2. 回填 README「技术决策文档」节

`README.md` 第 316 行起的占位文案（「正在整理…」）已替换为 4 章链接表。

### 3. 台账更新

- `T-G`：`待认领 → 待复核`（夜灯），备注记录交付内容。
- `T-F`：备注补充「远程 main 已推至 `3f28f4c`，CI 复验 Run `34007566771` success」。

## CI 复验结果（新 Run 34007566771）

- **状态：Success**（1m24s），触发 commit `3f28f4c`（docs: ledger），push 触发。
- 产出 artifact：`eval-report-4-34007566771`（3.17 KB）。
- **1 条 warning**：`Node.js 20 is deprecated` —— 来自 `actions/checkout@v4` / `actions/setup-python@v5` / `actions/upload-artifact@v4` 三个官方 action 仍跑在 Node 20，GitHub 已公告 2025-09-19 起强制迁移 Node 24。
  - **非功能性问题**，与 T-E 的「45 passed 1 warning」同源。
  - 建议后续（非阻塞）把这些 action 升到 `@v5` / 最新版以消除 deprecation warning。此项未列入阶段台账，可作阶段二顺带优化。

## 待失眠夜 / Codex

1. **复核 T-G**：核对 27 条决策是否与代码实现一致、数字是否准确（重点：`docs/decisions/01~04`）。有异议直接在台账 T-G 行或交接文档回复。
2. **T-F 收尾（仍阻塞）**：
   - 4 张截图（部署 / 聊天页 / 评测报告 MD / CI 绿徽章）→ 放 `docs/screenshots/`，规范见 `docs/screenshots/README.md`。
   - 截图齐后把 T-F 从「进行中（等截图）」改「已完成」。
3. **Mermaid 人工确认**：打开 https://github.com/shimianye/talk 确认「系统架构」是渲染成图而非代码块。
4. push 已完成（`3f28f4c` 已在远程），无需再推；但**本次 T-G 的 6 个新增/修改文件（`docs/decisions/*` + README + 台账）尚未提交**，由我 commit 后仍需你在本机 push（沙箱无 GitHub credential）。

## 尚未提交的文件（夜灯本地）

- `M docs/TASK-LEDGER.md`
- `M README.md`
- `?? docs/decisions/`（5 个新文件）
